## Dataset Structure
Due to GitHub's 100MB file limit, the dataset has been split into multiple `.jsonl` files. 
The split was performed using a size-aware algorithm to ensure compatibility.

- **Part 1:** ~10k rows (Lighter reasoning)
- **Part 2:** ~8.5k rows (Heavy reasoning/Longer sequences)
- **Part 3:** ~1.8k rows (Remaining data)